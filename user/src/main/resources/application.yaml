spring:
  application:
    name: user
  profiles:
    active: prod

#  datasource:
#    url: jdbc:h2:mem:testdb
#    driver-class-name: org.h2.Driver
#    username: sa
#    password:
#
#  h2:
#    console:
#      enabled: true
#      path: /h2-console
#
#  jpa:
#    hibernate:
#      ddl-auto: update
#    show-sql: true

  datasource:
    url: jdbc:mysql://localhost:3306/ECOMDB?createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
    hibernate:

  #config:
    #import: "classpath:application_${spring.profiles.active}.yaml"
    #import: configserver:http://localhost:8888

#  rabbitmq:
#    host: localhost
#    port: 5672
#    username: guest
#    password: guest
